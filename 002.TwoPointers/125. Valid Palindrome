class Solution(object):
    def isPalindrome(self, s):
        s1="".join(c.lower() for c in s if c.isalnum())
        i=0
        j=len(s1)-1
        while(i<j):
            if s1[i] is not s1[j]:
                return False
            i+=1
            j-=1
        return True
        
