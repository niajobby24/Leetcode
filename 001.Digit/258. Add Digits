class Solution(object):
    def addDigits(self, num):
        while num>9:
            total=0
            value=num
            while(value>0):
                total=total+(value%10)
                value=value//10
            num=total
        return num

        
